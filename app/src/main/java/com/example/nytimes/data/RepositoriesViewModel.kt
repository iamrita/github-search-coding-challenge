package com.example.nytimes.data

import androidx.lifecycle.LiveData
import androidx.lifecycle.viewModelScope
import androidx.lifecycle.MutableLiveData
import androidx.lifecycle.ViewModel
import kotlinx.coroutines.launch

class RepositoriesViewModel : ViewModel() {
//    private val repository = GithubRepository()
//
//    private val _repositories = MutableLiveData<GithubApi.RepositoryList>()
//    val repositories: LiveData<GithubApi.RepositoryList> = _repositories
//
//    fun getRepositories(username: String) {
//        viewModelScope.launch {
//            _repositories.value = repository.getRepositories(username)
//        }
//    }
}